<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_margin="8dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    card_view:cardCornerRadius="12dp"
    card_view:cardElevation="6dp">

    <LinearLayout
        android:orientation="vertical"
        android:padding="12dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Header with profile image and username -->
        <LinearLayout
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!-- User profile image -->
            <ImageView
                android:id="@+id/imageViewProfile"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:scaleType="centerCrop"
                android:background="@drawable/circle_background"
                android:src="@android:drawable/sym_def_app_icon" />

            <!-- Username text -->
            <TextView
                android:id="@+id/textViewUserName"
                android:layout_marginStart="8dp"
                android:textStyle="bold"
                android:textSize="16sp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>
        </LinearLayout>

        <!-- Post content text -->
        <TextView
            android:id="@+id/textViewPost"
            android:layout_marginTop="8dp"
            android:textSize="16sp"
            android:textStyle="bold"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <!-- Shared post container -->
        <LinearLayout
            android:id="@+id/sharedContainer"
            android:orientation="vertical"
            android:layout_marginTop="12dp"
            android:background="#EEEEEE"
            android:padding="8dp"
            android:visibility="gone"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!-- Card for nested shared content -->
            <androidx.cardview.widget.CardView
                android:layout_marginTop="6dp"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                card_view:cardCornerRadius="12dp"
                card_view:cardElevation="6dp">

                <LinearLayout
                    android:orientation="vertical"
                    android:padding="12dp"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content">

                    <!-- Original shared post text -->
                    <TextView
                        android:id="@+id/textViewSharedContent"
                        android:textColor="#555555"
                        android:textStyle="italic"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"/>
                </LinearLayout>
            </androidx.cardview.widget.CardView>
        </LinearLayout>

        <!-- Optional post location -->
        <TextView
            android:id="@+id/textViewPostLocation"
            android:layout_marginTop="4dp"
            android:textColor="@android:color/darker_gray"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:visibility="gone"/>

        <!-- Optional tagged people display -->
        <TextView
            android:id="@+id/textViewFeedTagged"
            android:layout_marginTop="4dp"
            android:textColor="@android:color/holo_blue_dark"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:visibility="gone"/>

        <!-- Optional post image -->
        <ImageView
            android:id="@+id/imageViewPost"
            android:layout_marginTop="8dp"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:scaleType="centerCrop"
            android:visibility="gone"/>

        <!-- Optional post video -->
        <VideoView
            android:id="@+id/videoViewPost"
            android:layout_marginTop="8dp"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:visibility="gone"/>

        <!-- Like and Share buttons row -->
        <LinearLayout
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginTop="12dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!-- Like button with icon -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/buttonLike"
                style="@style/Widget.MaterialComponents.Button.TextButton.Icon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Like (0)"
                app:icon="@drawable/ic_thumb_up"
                app:iconGravity="textStart"
                app:iconPadding="8dp"/>

            <!-- Hidden old comment button (legacy) -->
            <Button
                android:id="@+id/buttonComment"
                android:visibility="gone"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Old Comment (Hidden)"/>

            <!-- Share button with icon -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/buttonShare"
                style="@style/Widget.MaterialComponents.Button.TextButton.Icon"
                android:layout_marginStart="16dp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Share"
                app:icon="@drawable/ic_share"
                app:iconGravity="textStart"
                app:iconPadding="8dp"/>
        </LinearLayout>

        <!-- List of user comments below the post -->
        <LinearLayout
            android:id="@+id/layoutCommentList"
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

        <!-- New comment input and submit -->
        <LinearLayout
            android:id="@+id/commentInputSection"
            android:orientation="horizontal"
            android:layout_marginTop="6dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!-- Text input for comment -->
            <EditText
                android:id="@+id/editTextNewComment"
                android:layout_weight="1"
                android:hint="Write a comment..."
                android:inputType="text"
                android:minHeight="48dp"
                android:importantForAutofill="no"
                android:layout_width="0dp"
                android:layout_height="wrap_content"/>

            <!-- Button to submit comment -->
            <Button
                android:id="@+id/buttonSubmitComment"
                android:text="Post"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>
        </LinearLayout>

    </LinearLayout>
</androidx.cardview.widget.CardView>
